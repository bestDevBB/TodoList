<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
</head>
<body>
    <form action="/todo/getData" method="GET">
        <button type="submit">데이터 불러오기</button>
    </form>
    datalist!
    <% if(locals.data) { %>
        <% for(let i=0;i<data.length; i++) { %>
            content: <%= data[i].content %>, date: <%= data[i].date %><br>
        <% }%>
    <% }%>
</body>
</html>